function submitAnnotation(this) {
    // var base64Image = CanvasToBMP.toDataURL(document.getElementById('imgCanvas'));
    var name = this.name;

    console.log(name);

    // document.getElementById("imgBase64").value = base64Image;
    // document.getElementById("saveImage").submit();
}